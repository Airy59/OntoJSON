Check Carriers / carrier:

in the JSON schema version, Carriers has a property ResponsibleCarrierCode.

In RDF/OWL, you'd rather expect an object property referring to the responsible carrier (individual).

Actually, the ERA ontology refers to Responsible carriers via a datatype property "ResponsibleCarrierCode"; it does the same for IMs etc.

The un-ontological "style" is probably inherited from the TAF XSD (check)

